@model IEnumerable<SaferMS.Models.DB.Sexo>

@{
    ViewData["Title"] = "Index";
}

<h1>Sexo</h1>

<p>
    <a asp-action="Create">Crear nuevo sexo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Genero)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Genero)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSexo">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdSexo">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.IdSexo">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>

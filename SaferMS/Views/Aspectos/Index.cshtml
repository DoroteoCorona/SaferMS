@model IEnumerable<SaferMS.Models.DB.Aspecto>

@{
    ViewData["Title"] = "Index";
}

<h1>Aspectos</h1>

<p>
    <a asp-action="Create">Crear nuevo aspecto</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomAspecto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomAspecto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAspecto">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdAspecto">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAspecto">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>

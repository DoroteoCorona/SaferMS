@model IEnumerable<SaferMS.Models.DB.Comportamiento>

@{
    ViewData["Title"] = "Index";
}

<h1>Comportamientos</h1>

<p>
    <a asp-action="Create">Crear nuevo comportamiento</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomComportamiento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomComportamiento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdComportamiento">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdComportamiento">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.IdComportamiento">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>

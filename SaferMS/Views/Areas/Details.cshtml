@model SaferMS.Models.DB.Area

@{
    ViewData["Title"] = "Details";
}

<h1>Detalles</h1>

<div>
    <h4>Área</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NomArea)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NomArea)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.IdArea">Editar</a> |
    <a asp-action="Index">Regresar al registro de áreas</a>
</div>
